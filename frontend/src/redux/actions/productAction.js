import { createAction } from "@reduxjs/toolkit";

export const setOrderCart = createAction("CART");
export const setOtpVerified = createAction("VER");
export const setSearchResult = createAction("SEARCH");
